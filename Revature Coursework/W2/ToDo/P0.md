# Project 0 - Individual - extends AI.. Use to help not to do.

Project guidelines-- Vehicle Inventory Management Program
- Project presentation Friday 11/21
- Must demonstrate running application
- 5-7 minutes long

- Application must be written in Java
- must include persistence- in format of choice
- Architectural layers(repository and service layers), Should demonstrate proper separation of concerns
- Application must create or include at least 2 class objects/models(not repo layer or service layer) 
- Application should continue running until the user would like to stop/exit
- Application should handle errors gracefully and should not stop the entire application
- Persisted data must include multiple types of objects from application
- Persisted data must include at least one many to many relationship
- If persisting in SQL needs to be in third normal form

- Persistent - start app, creates data, when app stops, data is still accessible.
- data that carries over
- your project should be included in a public git repo
-- that repo should be a part of the cohort organization
-- (P0HammadO) -- should be the name of the repository
-- commit early, commit often
- application must include unit testing
--unit testing must include 10 individual tests
--mock any repo layer interaction(not testing repo layer, just mocking)
--unit tests should feature happy path and negative result tests, and edge cases
-- try test driven design(TDD) for one method and test


